// script.js

document.getElementById('login').addEventListener('click', function() {
    // Add the code to handle the reservation action, e.g., redirect to a reservation form page
    window.location.href = '/reservations';
});
